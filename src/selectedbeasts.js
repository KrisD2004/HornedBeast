import { useState } from "react";
import { Modal, ModalBody } from "react-bootstrap";

const [Modal, setSelectBeast] = useState(0)

function selectedbeasts (){
   setSelectBeast(Modal, ModalBody)
 }






export default selectedbeasts